import { createMuiTheme } from '@material-ui/core';

const appTheme = createMuiTheme(
    {
        spacing: 5,
        palette: {
            type: 'dark',
        }
    }
);

export default appTheme;